/**
 * [net 网络通信类]
 */
const net = {
  /**
   * [status 网络连接状态]
   * @param {[Object]} datas [传参]
   * @return {[]} []
   */
  status: (datas) => {
    return datas
  }
}

const $status = net.status

export {
  $status
}
